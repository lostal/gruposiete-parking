# ============================================
# CONFIGURACIÓN DE PRODUCCIÓN
# ============================================
# Variables de entorno para desplegar en Vercel/Digital Ocean

# MongoDB Atlas (Usa un cluster de producción diferente)
MONGODB_URI=mongodb+srv://<username>:<password>@<production-cluster>.mongodb.net/<production-database>?retryWrites=true&w=majority

# NextAuth (Autenticación)
# IMPORTANTE: Usa tu dominio de producción aquí
NEXTAUTH_URL=https://tu-dominio.com
# Genera un NUEVO secreto para producción con: openssl rand -base64 32
NEXTAUTH_SECRET=your-production-secret-here-generate-with-openssl-rand-base64-32

# Resend (Email) - OBLIGATORIO en producción
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@gruposiete.es

# Entorno
NODE_ENV=production

# IMPORTANTE: NO incluyas SEED_ADMIN_PASSWORD en producción
# Crea el usuario admin manualmente desde MongoDB Atlas
