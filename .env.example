# ============================================
# CONFIGURACIÓN DE DESARROLLO LOCAL
# ============================================
# Copia este archivo a .env.local y completa los valores

# MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# NextAuth (Autenticación)
NEXTAUTH_URL=http://localhost:3000
# Genera un secreto con: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32

# Resend (Email) - Opcional en desarrollo, obligatorio en producción
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
EMAIL_FROM=noreply@gruposiete.es

# Entorno
NODE_ENV=development

# Seed (Opcional - contraseña para crear usuario admin inicial)
# Solo se usa al ejecutar: npm run seed
SEED_ADMIN_PASSWORD=admin123
